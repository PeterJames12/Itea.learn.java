
/*
* if necessary use max and min element
private static int minElement(Node top) {

        if (top != null) {
            if (top.value < result) {
                result = top.value;
            }
            minElement(top.next);
        }
        return result;
    }

    private static int maxElement(Node top) {
        if (top != null) {
            if (top.value > result) {
                result = top.value;
            }
            maxElement(top.next);
        }
        return result;
    }
*/
